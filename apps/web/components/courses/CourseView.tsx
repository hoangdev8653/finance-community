'use client';

import React, { useState } from 'react';
import Link from 'next/link';
import { ArrowLeft, ArrowRight, BookOpen, CalendarDays, Check, CirclePlay, Clock3, Eye, GraduationCap, LockKeyhole, Sparkles } from 'lucide-react';
import { CourseDetailResponse, CourseLesson } from '@/types/course';
import { courseService } from '@/lib/courses/course-service';

interface CourseViewProps {
  initialData: CourseDetailResponse;
  slug: string;
  presentation?: { estimatedDurationMinutes: number | null; learningOutcomes: string[] };
}

const lessonDetails = [
  { duration: '12 phút', description: 'Nắm những khái niệm nền tảng trước khi bắt đầu hành trình.' },
  { duration: '15 phút', description: 'Kết nối kiến thức cốt lõi với các tình huống thực tế.' },
  { duration: '18 phút', description: 'Xem cách phân tích vấn đề và lựa chọn hướng xử lý phù hợp.' },
  { duration: '14 phút', description: 'Tổng hợp các ý chính để sẵn sàng áp dụng ngay.' },
];
const fallbackOutcomes = ['Hiểu những khái niệm cốt lõi của chủ đề', 'Nhận biết các tình huống thường gặp trong thực tế', 'Biết cách áp dụng kiến thức từng bước', 'Xây dựng tư duy chủ động và bền vững'];
const relatedCourses = [
  { title: 'Trí tuệ nhân tạo và tương lai của con người', lessons: 8, duration: '45 phút', tone: 'from-cyan-950 via-blue-800 to-cyan-300' },
  { title: 'Lịch sử thế giới hiện đại', lessons: 6, duration: '1 giờ 10 phút', tone: 'from-amber-950 via-orange-700 to-amber-200' },
  { title: 'Sức khỏe tinh thần', lessons: 5, duration: '50 phút', tone: 'from-rose-950 via-rose-700 to-orange-200' },
  { title: 'Kỹ năng giao tiếp hiệu quả', lessons: 4, duration: '40 phút', tone: 'from-slate-800 via-teal-700 to-emerald-200' },
];

function formatUpdatedAt(value: string) { return new Intl.DateTimeFormat('vi-VN', { month: 'long', year: 'numeric' }).format(new Date(value)); }
function formatDuration(minutes: number | null | undefined) { if (!minutes) return '~ 1 giờ 20 phút'; const hours = Math.floor(minutes / 60); const rest = minutes % 60; return `~ ${hours ? `${hours} giờ` : ''}${hours && rest ? ' ' : ''}${rest ? `${rest} phút` : ''}`; }

export function CourseView({ initialData, slug, presentation }: CourseViewProps) {
  const [lessons, setLessons] = useState<CourseLesson[]>(initialData.articles);
  const [currentPage, setCurrentPage] = useState(initialData.meta.page);
  const [hasNextPage, setHasNextPage] = useState(initialData.meta.hasNextPage);
  const [isLoadingMore, setIsLoadingMore] = useState(false);
  // Until progress comes from the learning-progress endpoint, preserve the initial mock state.
  const completedLessons = Math.min(3, initialData.articles.length);
  const progress = initialData.meta.totalItems ? Math.round((completedLessons / initialData.meta.totalItems) * 100) : 0;
  const duration = formatDuration(presentation?.estimatedDurationMinutes);
  const outcomes = presentation?.learningOutcomes.length ? presentation.learningOutcomes : fallbackOutcomes;

  const handleLoadMore = async () => {
    if (!hasNextPage || isLoadingMore) return;
    setIsLoadingMore(true);
    try {
      const response = await courseService.getBySlug(slug, { page: currentPage + 1, limit: 20 });
      const knownIds = new Set(lessons.map((lesson) => lesson.id));
      setLessons((previous) => [...previous, ...response.articles.filter((lesson) => !knownIds.has(lesson.id))]);
      setCurrentPage(response.meta.page);
      setHasNextPage(response.meta.hasNextPage);
    } finally { setIsLoadingMore(false); }
  };

  return <main id="main-content" className="course-detail min-h-screen bg-[#f7fbfa] text-slate-800 dark:bg-background">
    <style jsx>{`
      .course-detail > section:first-of-type > div:last-child,
      .course-detail > div,
      .course-detail > section:not(:first-of-type) { max-width: 1440px !important; }
    `}</style>
    <section className="relative isolate overflow-hidden border-b border-emerald-100 bg-gradient-to-br from-emerald-50 via-white to-cyan-50 dark:border-emerald-950 dark:from-slate-950 dark:via-slate-950 dark:to-emerald-950/30">
      <div aria-hidden className="absolute inset-y-0 right-0 -z-10 hidden w-1/2 overflow-hidden lg:block"><div className="absolute right-12 top-12 h-64 w-64 rounded-full border border-emerald-200/70 bg-white/30 shadow-[0_0_90px_rgba(16,185,129,0.13)]" /><div className="absolute right-40 top-28 grid h-48 w-48 place-items-center rounded-[2.5rem] border border-white/80 bg-white/60 shadow-xl shadow-emerald-950/5 backdrop-blur-sm dark:border-white/10 dark:bg-white/5"><GraduationCap className="h-20 w-20 text-emerald-700/80 dark:text-emerald-300" strokeWidth={1.25} /></div><div className="absolute bottom-8 right-8 h-28 w-72 rotate-[-7deg] rounded-3xl border border-emerald-100 bg-white/70 shadow-lg shadow-emerald-950/5 dark:border-white/10 dark:bg-white/5" /></div>
      <div className="mx-auto max-w-[1240px] px-4 py-8 sm:px-6 sm:py-12 lg:px-8 lg:py-14"><Link href="/khoa-hoc" className="inline-flex min-h-11 items-center gap-2 rounded-lg px-2 text-sm font-semibold text-slate-600 transition hover:bg-white/70 hover:text-emerald-800 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-600 dark:text-slate-300"><ArrowLeft className="h-4 w-4" /> Tất cả khóa học</Link><div className="mt-5 max-w-2xl"><span className="inline-flex items-center gap-1.5 rounded-full bg-emerald-700 px-3 py-1.5 text-xs font-bold tracking-wide text-white"><Sparkles className="h-3.5 w-3.5" /> KHÓA HỌC</span><h1 className="mt-4 font-heading text-3xl font-extrabold tracking-tight text-slate-950 sm:text-4xl lg:text-5xl dark:text-white">{initialData.series.name}</h1><p className="mt-4 max-w-xl text-base leading-7 text-slate-600 sm:text-lg dark:text-slate-300">{initialData.series.description || 'Một lộ trình ngắn gọn, giúp bạn xây kiến thức nền tảng và tự tin áp dụng vào thực tế.'}</p><div className="mt-5 flex flex-wrap gap-x-5 gap-y-3 text-sm font-semibold text-slate-600 dark:text-slate-300"><span className="inline-flex items-center gap-2"><BookOpen className="h-4 w-4 text-emerald-700" /> {initialData.meta.totalItems} bài học</span><span className="inline-flex items-center gap-2"><Clock3 className="h-4 w-4 text-emerald-700" /> ~ 1 giờ 20 phút</span><span className="inline-flex items-center gap-2"><CalendarDays className="h-4 w-4 text-emerald-700" /> Cập nhật {formatUpdatedAt(initialData.series.createdAt)}</span></div><div className="mt-7 max-w-md"><div className="flex items-center justify-between text-xs font-bold text-slate-600 dark:text-slate-300"><span>Tiến độ khóa học</span><span className="text-emerald-700 dark:text-emerald-400">{progress}% · {completedLessons}/{initialData.meta.totalItems || 0} bài hoàn thành</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-emerald-100 dark:bg-slate-800"><div className="h-full rounded-full bg-emerald-600 transition-all" style={{ width: `${progress}%` }} /></div></div><div className="mt-6 flex flex-wrap gap-3"><a href="#lo-trinh" className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-emerald-800 px-5 text-sm font-bold text-white shadow-lg shadow-emerald-900/15 transition hover:bg-emerald-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-600 focus-visible:ring-offset-2"><CirclePlay className="h-4 w-4" /> Tiếp tục học</a><a href="#gioi-thieu" className="inline-flex min-h-11 items-center rounded-xl border border-emerald-300 bg-white/70 px-5 text-sm font-bold text-emerald-800 transition hover:bg-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-600 dark:bg-slate-900/70 dark:text-emerald-300">Xem chi tiết</a></div></div></div>
    </section>
    <div className="mx-auto grid max-w-[1240px] gap-8 px-4 py-10 sm:px-6 lg:grid-cols-[minmax(0,1fr)_300px] lg:px-8 lg:py-14"><div className="min-w-0 space-y-10"><section id="gioi-thieu" aria-labelledby="about-course"><h2 id="about-course" className="flex items-center gap-2 font-heading text-xl font-bold text-slate-900 dark:text-white"><BookOpen className="h-5 w-5 text-emerald-700" /> Giới thiệu về series</h2><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-600 sm:text-base dark:text-slate-300">{initialData.series.description || 'Khóa học được thiết kế theo từng bài ngắn, rõ ràng và có trình tự. Bạn có thể học theo nhịp độ của mình, quay lại bất cứ lúc nào và dần hình thành nền tảng vững chắc.'}</p><div className="mt-5 flex min-h-40 items-end overflow-hidden rounded-2xl border border-emerald-100 bg-gradient-to-br from-[#073b3a] via-[#0d6962] to-[#8ad6c9] p-6 shadow-sm dark:border-emerald-900"><div><p className="text-xs font-bold tracking-[0.16em] text-emerald-100">BREWSEVEN LEARNING</p><p className="mt-2 max-w-sm font-heading text-xl font-bold text-white">Học từng bước, hiểu thật sâu.</p></div><Sparkles aria-hidden className="ml-auto h-16 w-16 text-white/60" strokeWidth={1} /></div></section><section id="lo-trinh" aria-labelledby="curriculum"><div className="flex items-end justify-between gap-4"><h2 id="curriculum" className="flex items-center gap-2 font-heading text-xl font-bold text-slate-900 dark:text-white"><GraduationCap className="h-5 w-5 text-emerald-700" /> Lộ trình học tập</h2><span className="text-xs font-semibold text-slate-500">{initialData.meta.totalItems} bài học</span></div><ol className="mt-5 space-y-3 border-l border-emerald-100 pl-5 dark:border-emerald-950">{lessons.map((lesson, index) => <LessonRow key={lesson.id} lesson={lesson} index={index} seriesSlug={slug} completed={index < completedLessons} locked={index > completedLessons} />)}</ol>{lessons.length === 0 && <div className="mt-5 rounded-2xl border border-dashed border-slate-300 bg-white p-8 text-center dark:border-slate-700 dark:bg-slate-900"><BookOpen className="mx-auto h-7 w-7 text-slate-400" /><p className="mt-3 font-semibold">Khóa học chưa có bài học được xuất bản.</p></div>}{hasNextPage && <div className="mt-6 text-center"><button type="button" onClick={() => void handleLoadMore()} disabled={isLoadingMore} className="min-h-11 rounded-xl border border-emerald-300 bg-white px-5 text-sm font-bold text-emerald-800 transition hover:bg-emerald-50 disabled:opacity-60 dark:bg-slate-900 dark:text-emerald-300">{isLoadingMore ? 'Đang tải…' : 'Xem thêm bài học'}</button></div>}</section></div><aside className="space-y-4 lg:sticky lg:top-6 lg:self-start"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 className="font-heading text-base font-bold text-slate-900 dark:text-white">Thông tin series</h2><dl className="mt-4 space-y-4 text-sm text-slate-600 dark:text-slate-300"><InfoRow icon={<BookOpen />} label={`${initialData.meta.totalItems} bài học`} /><InfoRow icon={<Clock3 />} label={duration} /><InfoRow icon={<CalendarDays />} label={`Cập nhật ${formatUpdatedAt(initialData.series.createdAt)}`} /></dl></section><section className="overflow-hidden rounded-2xl border border-emerald-100 bg-emerald-50/50 p-5 dark:border-emerald-950 dark:bg-emerald-950/20"><h2 className="font-heading text-base font-bold text-slate-900 dark:text-white">Bạn sẽ học được gì?</h2><ul className="mt-4 space-y-3">{outcomes.map((outcome) => <li key={outcome} className="flex gap-2 text-sm leading-5 text-slate-600 dark:text-slate-300"><span className="mt-0.5 grid h-5 w-5 shrink-0 place-items-center rounded-full bg-emerald-100 text-emerald-700 dark:bg-emerald-900 dark:text-emerald-300"><Check className="h-3 w-3" /></span>{outcome}</li>)}</ul><div aria-hidden className="mt-7 flex h-28 items-end justify-center rounded-xl bg-gradient-to-br from-white to-emerald-100/80 dark:from-slate-900 dark:to-emerald-950"><BookOpen className="mb-6 h-12 w-12 text-emerald-700/60" strokeWidth={1.25} /></div></section></aside></div>
    <section className="mx-auto max-w-[1240px] px-4 sm:px-6 lg:px-8"><div className="overflow-hidden rounded-2xl bg-gradient-to-r from-emerald-50 via-cyan-50 to-emerald-100 p-6 sm:p-8 dark:from-emerald-950/50 dark:via-slate-900 dark:to-emerald-950/40"><div className="flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-heading text-xl font-bold text-slate-900 dark:text-white">Hành trình tri thức không có giới hạn</h2><p className="mt-2 max-w-xl text-sm leading-6 text-slate-600 dark:text-slate-300">Mỗi series mở ra một chủ đề mới để bạn khám phá, học hỏi và phát triển mỗi ngày.</p></div><Link href="/khoa-hoc" className="inline-flex min-h-11 shrink-0 items-center justify-center gap-2 rounded-xl border border-emerald-500 bg-white px-4 text-sm font-bold text-emerald-800 transition hover:bg-emerald-50 dark:bg-slate-900 dark:text-emerald-300">Khám phá series khác <ArrowRight className="h-4 w-4" /></Link></div></div></section>
    <section className="mx-auto mb-12 max-w-[1240px] px-4 py-10 sm:px-6 lg:px-8" aria-labelledby="related-courses"><div className="flex items-center justify-between gap-4"><h2 id="related-courses" className="flex items-center gap-2 font-heading text-lg font-bold text-slate-900 dark:text-white"><Sparkles className="h-5 w-5 text-emerald-700" /> Có thể bạn cũng quan tâm</h2><Link href="/khoa-hoc" className="inline-flex min-h-11 items-center gap-1 text-xs font-bold text-emerald-800 hover:text-emerald-600 dark:text-emerald-300">Xem tất cả series <ArrowRight className="h-3.5 w-3.5" /></Link></div><div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{relatedCourses.map((course) => <Link key={course.title} href="/khoa-hoc" className="group overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-0.5 hover:border-emerald-300 hover:shadow-md focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-600 dark:border-slate-800 dark:bg-slate-900"><div className={`flex h-24 items-end justify-end bg-gradient-to-br p-4 ${course.tone}`}><BookOpen className="h-8 w-8 text-white/80" strokeWidth={1.25} /></div><div className="p-4"><h3 className="min-h-10 font-heading text-sm font-bold leading-5 text-slate-900 group-hover:text-emerald-700 dark:text-white dark:group-hover:text-emerald-400">{course.title}</h3><p className="mt-3 flex items-center gap-3 text-xs font-medium text-slate-500"><span className="inline-flex items-center gap-1"><BookOpen className="h-3.5 w-3.5" />{course.lessons} bài học</span><span>~ {course.duration}</span></p></div></Link>)}</div></section>
  </main>;
}

function LessonRow({ lesson, index, seriesSlug, completed, locked }: { lesson: CourseLesson; index: number; seriesSlug: string; completed: boolean; locked: boolean }) {
  const detail = lessonDetails[index % lessonDetails.length]; const href = `/khoa-hoc/${encodeURIComponent(seriesSlug)}/${encodeURIComponent(lesson.slug)}`;
  const number = String(index + 1).padStart(2, '0');
  return <li className="relative"><span className={`absolute -left-[38px] top-7 grid h-8 w-8 place-items-center rounded-full border-2 text-[10px] font-extrabold shadow-sm ${completed ? 'border-emerald-600 bg-emerald-600 text-white' : locked ? 'border-slate-200 bg-slate-100 text-slate-400 dark:border-slate-700 dark:bg-slate-800' : 'border-emerald-600 bg-emerald-600 text-white'}`}>{locked ? <LockKeyhole className="h-3.5 w-3.5" /> : number}</span><article className={`group flex flex-col gap-4 rounded-2xl border bg-white p-4 shadow-sm transition sm:flex-row sm:items-center dark:bg-slate-900 ${locked ? 'border-slate-200/80 opacity-75 dark:border-slate-800' : 'border-slate-200 hover:border-emerald-300 hover:shadow-md dark:border-slate-800'}`}><div className="grid h-20 w-full shrink-0 place-items-center rounded-xl bg-gradient-to-br from-emerald-950 via-teal-700 to-cyan-300 text-white sm:w-28"><BookOpen className="h-8 w-8 opacity-85" strokeWidth={1.25} /></div><div className="min-w-0 flex-1"><h3 className="font-heading text-base font-bold text-slate-900 group-hover:text-emerald-700 dark:text-white dark:group-hover:text-emerald-400">{lesson.title}</h3><p className="mt-1 text-sm leading-5 text-slate-600 dark:text-slate-300">{detail.description}</p><div className="mt-3 flex flex-wrap gap-x-4 gap-y-2 text-xs font-medium text-slate-500"><span className="inline-flex items-center gap-1.5"><Clock3 className="h-3.5 w-3.5" />{detail.duration}</span><span className="inline-flex items-center gap-1.5"><Eye className="h-3.5 w-3.5" />{lesson.viewCount} lượt xem</span></div></div>{locked ? <span className="inline-flex min-h-10 items-center gap-1.5 self-end rounded-lg bg-slate-100 px-3 text-xs font-bold text-slate-500 sm:self-center dark:bg-slate-800"><LockKeyhole className="h-3.5 w-3.5" />Đang khóa</span> : <Link href={href} className="inline-flex min-h-10 items-center gap-1.5 self-end rounded-lg bg-emerald-800 px-3 text-xs font-bold text-white transition hover:bg-emerald-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-600 sm:self-center">{completed ? 'Học lại' : 'Học bài'} <ArrowRight className="h-3.5 w-3.5" /></Link>}</article></li>;
}
function InfoRow({ icon, label }: { icon: React.ReactNode; label: string }) { return <div className="flex items-center gap-3"><span className="text-emerald-700 dark:text-emerald-400">{icon}</span><dd>{label}</dd></div>; }
